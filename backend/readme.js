//   $or + $in oprator ka syntex
// find({$or: [{key: {$regex: "text", options: "i"}}, {key: {$regex: "text", options: "i"}],
//  key: {$in:["value1", "value2"]}, })

// $in oprator syntex
// find({ key: { $in: ["value1", "value2"] } });
